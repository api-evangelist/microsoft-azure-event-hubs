naftiko: 1.0.0-alpha2
info:
  label: Azure Event Hubs Management REST API — Operations
  description: 'Azure Event Hubs Management REST API — Operations. 1 operations. Lead operation: Azure Event Hubs List available
    operations. Self-contained Naftiko capability covering one Microsoft Azure Event Hubs business surface.'
  tags:
  - Microsoft Azure Event Hubs
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_EVENT_HUBS_API_KEY: MICROSOFT_AZURE_EVENT_HUBS_API_KEY
capability:
  consumes:
  - type: http
    namespace: azure-event-hubs-management-operations
    baseUri: https://management.azure.com
    description: Azure Event Hubs Management REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.EventHub-operations
      path: /providers/Microsoft.EventHub/operations
      operations:
      - name: operationslist
        method: GET
        description: Azure Event Hubs List available operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_EVENT_HUBS_API_KEY}}'
  exposes:
  - type: rest
    namespace: azure-event-hubs-management-operations-rest
    port: 8080
    description: REST adapter for Azure Event Hubs Management REST API — Operations. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-eventhub/operations
      name: providers-microsoft-eventhub-operations
      description: REST surface for providers-Microsoft.EventHub-operations.
      operations:
      - method: GET
        name: operationslist
        description: Azure Event Hubs List available operations
        call: azure-event-hubs-management-operations.operationslist
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: azure-event-hubs-management-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Event Hubs Management REST API — Operations. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: azure-event-hubs-list-available
      description: Azure Event Hubs List available operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: azure-event-hubs-management-operations.operationslist
      outputParameters:
      - type: object
        mapping: $.
